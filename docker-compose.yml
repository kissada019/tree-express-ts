services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: treedb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: 1234
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
